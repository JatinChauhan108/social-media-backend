<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="flex justify-end w-full">
            <a class="bg-red-400 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3">
            <div class="h-10 w-10 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h3 class = "text-3xl"><span class = "font-light"> Hello </span>, <%= user.name %>👋🏻</h3>
        </div>
        <h5 class="mb-5 mt-3">You can create a new post</h5>
        <form method="post" action="/post">
            <textarea placeholder="What's on your mind ?" class = "block w-1/3 p-3 resize-none outline-none border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
            <input class = "w-40 block mt-2 rounded-md bg-blue-500 px-3 py-2 text-sm" type="submit" value="Create new post">
        </form>
        <div class = "posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 p-4 rounded-md border-2 mb-4 border-zinc-700 bg-zinc-800">
                        <h4 class="text-blue-400 mb-2">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight"><%= post.content %></p>
                        <small class="mt-4 block"><%= post.likes.length %></small>
                        <div class="btns flex gap-4 mt-1">
                            <a class="text-blue-400" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Dislike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>